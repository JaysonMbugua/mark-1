/*Configures nextjs*/

/*imports a typscript type defintion from nextjs*/

import type { NextConfig } from "next";

/*Specifiy desired configurations for nextJS*/

const nextConfig: NextConfig = {
  /* config options here */
};

/*allows other parts of project to use configurations*/

export default nextConfig;
